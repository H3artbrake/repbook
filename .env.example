# Replace with the exact address from tailscale serve status (no trailing path).
APP_ORIGIN=https://your-nas.your-tailnet.ts.net
COOKIE_SECURE=true
# For compose.nas.yaml, set your GitHub image address (lowercase).
REPBOOK_IMAGE=ghcr.io/h3artbrake/repbook:latest
